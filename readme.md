# Intro

This is a full stack exercise management collaborative, modern,
AI, sci-fi, global, distributed, secure, sleek, aerodynamic system.

A sqLITE database is used on the backend to store the data, using a
python CLI program to access the database, and a webserver that runs
the python CLI program to read and write to the database.
